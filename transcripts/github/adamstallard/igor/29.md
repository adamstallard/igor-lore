{
  "item": "github:adamstallard/igor#29",
  "url": "https://github.com/adamstallard/igor/issues/29",
  "role": "generalist",
  "outcome": "produced",
  "transcript": "I've implemented issue #29. Summary of the change:\n\n**Core mechanism** (`src/budget.ts`): added `TokenSource` (`tokenEnv` / `tokenFile` / `tokenCommand`) and one `resolveToken()` function that both `readUsage` and `workerEnv` now call, replacing their previously-duplicated `env[tokenEnv]` lookups. `parseOrgBudget` parses the two new seat fields and rejects a seat naming more than one. A seat naming none of the three is unchanged — still falls back to whatever login is ambient, exactly as before.\n\n`token_command` runs via a real subprocess (`spawn(..., { shell: true, timeout })`), with a provisional 10s timeout so a command blocked on an interactive prompt (`pass`/`gpg` with no TTY, `op read` wanting a fresh sign-in) fails loudly instead of hanging `igor budget` or a worker spawn forever.\n\n**Threaded through**: `Gate.tokenEnv` → `Gate.token: TokenSource`; `ExecuteOptions.seatTokenEnv` → `seatToken: TokenSource`; `workerEnv` is now `async`. `src/loop.ts` updated to match.\n\n**Docs**: README, `docs/deployment.md`, `docs/seats.md`, `docs/architecture.md`, `igor.config.example.yaml`, `deploy/igor.service` (commented `LoadCredential=` example), and `deploy/env.example` all updated in place — no wholesale rewrites, just the specific sentences that were now wrong (notably `deployment.md`'s old \"Igor cannot read it yet: #29\" paragraph, which now describes the working recipe).\n\n**OpenSpec**: new change `openspec/changes/seat-token-sources/` with proposal, tasks, and spec deltas for `seat-budget` and `task-execution`. Left unarchived — that's a post-review step per `AGENTS.md`.\n\n**Tests**: added for each new source resolving, each failing loudly (unset var, unreadable/empty file, failing/silent/hanging command), the \"exactly one\" validation, and the `token_file`/`token_command` paths reaching a worker's environment.\n\nOne caveat I can't close myself: **this sandbox has no network access, so `npm install` never ran, and neither `npm run typecheck` nor `vitest run` executed here.** I reviewed every change by hand, including the `exactOptionalPropertyTypes` spots (the `resolveToken` file branch, the spread-built `token: {...}` in `budgetGate`, and `budget: gate` flowing into `loop.ts`'s inline type), but a human should run both before merging. The `LoadCredential=`/`$CREDENTIALS_DIRECTORY` recipe in the docs is written from systemd's documented contract, not verified live against a real systemd unit — also worth a real check before relying on the exact wording."
}
