{
  "item": "github:adamstallard/igor#34",
  "url": "https://github.com/adamstallard/igor/issues/34",
  "role": "generalist",
  "outcome": "produced",
  "transcript": "Implemented `src/capacity.ts` and `test/capacity.test.ts` for issue #34 (§1 of `capacity-from-observation`), and ticked tasks 1.1–1.4 in that change's `tasks.md` since this implements exactly that scope.\n\n**What's in `src/capacity.ts`:**\n- `Observation` interface — exactly the shape the item specifies (`at`, `seat`, `window`, `percentUsed`, `resetsAt?`, `resetsPhrase?`, `source`, `model?`).\n- `resolveReset(phrase, at)` — parses `\"Sep 18 at 4pm (America/Los_Angeles)\"`-style phrases to an ISO instant, using `Intl.DateTimeFormat` with `timeZoneName: 'longOffset'` for the zone offset (no new dependency). Returns `undefined` for anything that doesn't match the shape or names a zone `Intl` doesn't know. Handles the missing year by taking the first occurrence at or after `at` (this year in the zone, or next year).\n- `CAPACITY_PATH = 'capacity.ndjson'`, `recordObservation` (appends via injected `write`, defaulting to `appendRecord`, so it lands in `capacity/<date>.ndjson`), `loadObservations` (mirrors `loadSpend`).\n\n**Tests** cover both sources producing one shape (now built independently through `resolveReset` rather than one derived from the other, so the key-set assertion is load-bearing), an unresolvable reset keeping its phrase with no `resetsAt` key at all, a per-model observation, append-not-replace for a second observation, and the resolver against the three real phrases plus `:30`, am/pm, midnight/noon, year rollover, an unknown zone, and junk input.\n\n**One caveat, worth flagging plainly:** this sandbox has no `node_modules` and `npm install` is blocked (no network), so I could not run `npm run typecheck` or `vitest` here. I hand-traced the timezone arithmetic against all the phrase cases (confirmed independently against a throwaway script before deleting it) and checked the `exactOptionalPropertyTypes`/`noUncheckedIndexedAccess` implications against existing patterns in `budget.ts`/`state.ts`/`execute.ts`, including one real bug an advisor review caught (passing the `Observation` interface directly where `Record<string, unknown>` was expected — fixed by spreading into a fresh object literal). CI is the real verification here."
}
