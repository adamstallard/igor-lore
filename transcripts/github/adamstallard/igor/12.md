{
  "item": "github:adamstallard/igor#12",
  "url": "https://github.com/adamstallard/igor/issues/12",
  "role": "generalist",
  "outcome": "produced",
  "transcript": "I've made the change to `igor.config.example.yaml` and two directly-related stale doc references, verified there's nothing else live to update, and got a second-pass review. Here's the summary:\n\n## Change made\n\n**`igor.config.example.yaml`**: deleted the `scoring:` block (`halfLifeDays: 365`). `resolveConfig` in `src/config.ts` never read it, and `src/scoring.ts` explicitly documents why: the decay term was deliberately removed — lore is mined from old review comments and is old by construction, so a decay curve reports the whole store as stale without saying anything useful. `support`/`expertSupport` are derived straight from provenance; there's nothing left for `halfLifeDays` to configure. Archived change `2026-09-14-lore-retrieval` (task 1.2) already removed the field from config — the example file just never followed.\n\n**`README.md`** and **`docs/architecture.md`**: both had a matching stale line listing \"decay half-life\" as a shared team config decision, in the same config-field-list style as the example. Since I was touching this exact fact, I dropped the same phrase from both rather than leave the docs and the example disagreeing about what's in the config.\n\nNo spec delta needed — `openspec/specs/**` has no scoring/half-life requirement; this is dead example/doc text, not a behavior change.\n\n## Two related loose ends I did NOT touch (flagging, not fixing — these are judgment calls)\n\n1. `openspec/changes/lore-from-reviews/specs/lore-consolidation/spec.md` (a pending, unimplemented change) still requires a \"configured half-life\" — but that's a *different* half-life, for cluster scoring during review-mining consolidation, not per-entry recency. It doesn't conflict with this deletion.\n2. Those same README/architecture.md sentences also list \"repositories in scope\" as a config field, which `resolveConfig` doesn't actually have. Same family of drift as #12, but whether that's planned-but-unbuilt or genuinely dead is a call for someone who knows the roadmap, not me.\n\nDiff is 3 files, all deletions of stale text — nothing added, nothing reformatted."
}
