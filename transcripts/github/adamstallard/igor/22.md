{
  "item": "github:adamstallard/igor#22",
  "url": "https://github.com/adamstallard/igor/issues/22",
  "role": "generalist",
  "outcome": "produced",
  "transcript": "## Summary\n\nThe item's first option — have the template record rejections itself — is closed, not just harder than it looks: a candidate the reviewer deletes on the proposal branch never reaches `main` at all, so no diff of `main` (`before..HEAD`, any `--diff-filter`) can see it. It's invisible, not ambiguous. And a deleted `entries/*.md` file that *does* show up in that diff means something else entirely — an entry retired or superseded on `main` — which is exactly the case `reconcile.ts` guards against misreading as a rejection (see its `missingLocally`/`wasLanded` handling and the test \"never rejects what the pull request landed\"). Recording rejections in CI for real would mean re-deriving proposed-vs-landed from the PR API inside the workflow under a different auth model — a design decision, not a docs fix.\n\nSo I took the item's second option: corrected the two places that state or imply the workflow is a substitute for `reconcile`.\n\n- `templates/promote-on-merge.yml` — expanded the header comment to say the workflow only promotes, explain concretely why widening `--diff-filter` to `D` wouldn't work (the invisible-deletion / retirement-looks-like-rejection distinction above), and state plainly that installing it doesn't replace running `reconcile`.\n- `README.md` — the \"Merge-triggered promotion\" setup paragraph now says the workflow only promotes and that a destination still needs `reconcile` run against it for `rejected/<id>.md` to get written, or a deleted candidate stays proposable. Kept it to the operational fact (the mechanism lives in the template instead) so the \"Neither... both...\" paragraph right after still reads correctly against its original two antecedents.\n\nNo spec delta needed — `openspec/specs/lore-review/spec.md`'s reconciliation requirement already assigns rejection-recording to `reconcile`, and its \"own automation at merge time\" clause covers promotion only. The README now agrees with the spec rather than changing it. No code or tests touched, since nothing about actual behavior changed.\n\nOne thing I noticed but left alone, worth a second look: `src/propose.ts:123`'s PR body tells reviewers entries \"are promoted to `active` on the next run, which also records anything you deleted.\" On a workflow-only destination, promotion no longer waits for a run — it happens automatically — but the \"which also records anything you deleted\" part still implicitly assumes a `reconcile` run happens. That's the PR-body promise the item quotes, and it's governed by an OpenSpec requirement with its own scenarios (\"The pull request body states the review contract\"), so wording it differently is a behavior change needing its own change proposal — flagging it rather than touching it here."
}
