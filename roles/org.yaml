# Inherited by every role here. A role may narrow any of this and widen none of it.
allow: [comment, draft-pr, unassign]

# What a worker may run. Without these it can edit files and nothing else, so it cannot
# build, test or type-check its own change — which is how a pull request arrived hand-traced
# rather than verified. Read from here and never from the repository being worked, since that
# repository's settings are a file the worker can edit.
#
# A worker gets a fresh shallow clone with no `node_modules`, so `npm ci` is what makes every
# other entry here mean anything: without it `npm test` runs in a tree with nothing installed.
# Measured at 0.97s against a warm cache, and npm declines dependency install scripts by
# default, so this does not hand them the seat's token.
#
# A compound command is checked part by part, so `npm run build 2>&1 | tail -100` needs `tail`
# as well as `npm run build`. Both were refused before they were listed here.
commands:
  - "npm ci:*"
  - "npm test:*"
  - "npm run build:*"
  - "npm run typecheck:*"
  - "npx vitest run:*"
  - "npx tsc --noEmit:*"
  - "npx openspec validate:*"
  - "git diff:*"
  - "git status:*"
  - "tail:*"
  - "head:*"
  - "wc:*"
completion: unassign
# Every role must name a seat wherever seats are declared, so this is inherited rather than
# repeated. One seat today; a role drawing on a different subscription overrides it.
seat: adam
lane:
  labels:
    excludes: [Human, question, wontfix, blocked]
instructions: |
  Keep diffs minimal, in prose as much as in code. Do not reformat what you did not need to
  touch, and do not restate a README or a doc in your own words on the way past — a claim
  about how the system works is somebody's considered wording, and rewriting it while fixing
  something else buries the change you were asked for. Document what you added; leave the rest.
  Write for the person who will read it on their own issue, mid-task. Be concise and be
  useful — a short answer with the change in it beats a long one about the change. Where
  what you have to say is too long to read in a comment, say what you would do and offer to
  open a draft instead of pasting it. Judge that yourself; there is no line count.
  Verify against the real thing rather than reasoning about it — every substantial finding in
  this project came from a live call contradicting an assumption.
